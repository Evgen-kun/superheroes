name: superheroes
description: A flutter superheroes project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  rxdart: ^0.27.7
  provider: ^6.0.5
  google_fonts: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  network_image_mock: ^2.1.1

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - google_fonts/
    - assets/images/
